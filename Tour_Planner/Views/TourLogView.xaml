<UserControl x:Class="Tour_Planner.Views.TourLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tour_Planner.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" 
           Margin="5" 
           ItemsSource="{Binding Data}" 
           AutoGenerateColumns="False" 
           AlternatingRowBackground="LightGray"
           CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding DateTime}" Header="Date/Time" Width="200"/>
                <DataGridTextColumn Binding="{Binding Comment}" Header="Comment"/>
                <DataGridTextColumn Binding="{Binding Difficulty}" Header="Difficulty"/>
                <DataGridTextColumn Binding="{Binding TotalDistance}" Header="Total Distance"/>
                <DataGridTextColumn Binding="{Binding TotalTime}" Header="Total Time"/>
                <DataGridTextColumn Binding="{Binding Rating}" Header="Rating"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
